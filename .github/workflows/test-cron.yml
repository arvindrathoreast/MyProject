name: Test Cron

on:
  workflow_dispatch:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Print Event
        run: |
          echo "Event = ${{ github.event_name }}"
          date
